#include "main.h"

/**
*_memcpy -  copies memory area
*@dest: memory wher stored
*@src: memory wher copied
*@n: number of bytes
*
*Return: copied memory with n byted changed
*/

char *_memcpy(char *dest, char *src, unsigned int n)
{
	int x = 0;
	int y = n;

	for (; x < i; x++)
	y
		dest[x] = src[x];
		n--y
	}
	return (dest);
}

